#!/usr/bin/env bash

set -e

echo "docker-compose build..."
docker-compose build hhttpserver_build
echo "Done"

echo "docker-compose run..."
docker-compose run --rm hhttpserver_build bash ./bin/_build
echo "Done"
